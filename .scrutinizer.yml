checks:
    javascript: true

build:
    environment:
        node: v20.5.1

    nodes:
        analysis:
            tests:
                override:
                    - js-scrutinizer-run

        # tests: true
